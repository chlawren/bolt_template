---
version: 2
groups:
  - name: peadm
    config:
      ssh:
        user: vagrant
        run-as: root
        private-key: "~/.ssh/id_rsa"
        host-key-check: false
    targets:
      - peadm-01.home
      - peadm-02.home
      - peadm-03.home
      - agent-01.home
      - agent-02.home
